# Critic Program

Review the generated bundle for correctness, missing files, regressions, and contract violations.

Rules:
- Persist reusable branch-quality judgments, not just pass/fail.
- Call out whether the branch violated any policy trace or repeated a known weak branch memory.
