# Playbook

- `init`: create ledger, prompts, reports, and root docs.
- `tick`: finish runs, advance stages, update reports.
- `start-next`: launch the next runnable work item.
- `watch`: keep the workspace alive on cadence.
- `build-submission`: package a CPU-first bundle and dry-run it locally.
