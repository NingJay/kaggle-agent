# Model Placeholder

Populate this directory with model artifacts such as:

`bird-vocalization-classifier-tensorflow2-perch_v2-v2/`

The bootstrap runtime only inventories this directory today. Real Perch-backed training should use the same path.

